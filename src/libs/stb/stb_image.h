/*
 * stb_image.h — bundled via GitHub Actions (downloaded during build)
 * Source: https://github.com/nothings/stb
 * Single-header image loader (JPG, PNG, BMP, GIF, TGA, PNM), public domain.
 *
 * The actual file is fetched by the CI workflow before compilation.
 */
#pragma once
// Implementation is compiled once in image.cpp via STB_IMAGE_IMPLEMENTATION
